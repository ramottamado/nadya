{% assign site_url = site.url | absolute_url -%}
{% assign page_url = page.url | absolute_url -%}
{% assign title = page.title | default: site.title -%}
{% assign author = page.author | default: site.author -%}
{% assign page_description = page.description | strip_html | normalize_whitespace -%}
{% assign page_excerpt = page.excerpt | strip_html | strip_newlines | truncate: 160 -%}
{% assign site_description = site.description | strip_html | normalize_whitespace -%}
{% assign description = page_description | default: page_excerpt | default: site_description -%}
{% assign logo = site.logo | absolute_url -%}
{% assign image = page.image | default: logo -%}
{% assign rel_icon = site.icon | relative_url -%}
{% assign rel_logo = site.logo | relative_url -%}
{% assign twitter_card = 'summary' -%}
{% if page.image -%}
{% assign twitter_card = 'summary_large_image' -%}
{% endif -%}